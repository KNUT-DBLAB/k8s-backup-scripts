apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-test
  labels:
    pvc: test
spec:
  storageClassName: local-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 4Gi
